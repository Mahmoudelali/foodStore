import React from 'react';
import { StyleSheet } from 'react-native';
import langs from './translation.json';

const Localize = () => {
	return <Text>{}</Text>;
};

const styles = StyleSheet.create({});

export default Localize;
